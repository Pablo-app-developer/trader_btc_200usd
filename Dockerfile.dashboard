FROM python:3.10-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir streamlit plotly flask psutil pyyaml requests pandas

# Expose ports
EXPOSE 8501 5000

# Start both services
CMD python healthcheck.py & streamlit run dashboard.py --server.port 8501 --server.address 0.0.0.0
